{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"PLUGIN_ROOT=${CLAUDE_PLUGIN_ROOT}\" >> /tmp/hook-debug.log && echo \"Script exists: $(test -f ${CLAUDE_PLUGIN_ROOT}/scripts/sync-kb.sh && echo YES || echo NO)\" >> /tmp/hook-debug.log",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
